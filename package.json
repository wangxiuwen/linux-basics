{
  "name": "hexo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npx gitbook build",
    "serve": "gitbook serve"
  },
  "dependencies": {},
  "devDependencies": {
    "gitbook-cli": "^2.3.2"
  }
}
